# Spec Management

## Spec Cleanup

When creating a new spec, check for existing specs in `.kiro/specs/`. If found, ask user if they want to remove old spec(s) to keep workspace clean (preserves in git history). User may decline if working on multiple features.
